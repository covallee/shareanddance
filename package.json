{
  "private": true,
  "name": "shareanddance-blog",
  "version": "2.0.0",
  "scripts": {
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --parallel",
    "build-studio": "cd studio && npm install && npm run build",
    "build-web": "npm install && (cd web && npm run build)"
  },
  "devDependencies": {
    "@sanity/cli": "^3.18.1",
    "lerna": "7.4.1"
  }
}
